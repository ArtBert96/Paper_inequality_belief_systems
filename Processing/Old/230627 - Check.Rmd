---
title: "Untitled"
output: html_document
date: "2023-06-27"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
load(here("Output",  "230627 - Data_New.RData"))
load(here("Output",  "230627 - Data_Old.RData"))
```

```{r}
data_us_total = data_us_old %>% 
  anti_join(data_us, by = "nomem_encr")

data_nl_total = data_nl_old %>% 
  anti_join(data_nl, by = "nomem_encr")

frq(data_us_old,cca)
frq(data_us_total,cca)

frq(data_nl_old,cca)
frq(data_nl_total,cca)

data_us_total %>% 
  select(p_income:ib_sex) %>% 
  as.vector() %>% 
  sd()

data_nl_total %>% 
  select(p_income:ib_sex) %>% 
  as.vector() %>% 
  sd()
```

